name: render_api
description: A typed Dart client for the Render REST API, covering workflows, tasks, and task runs.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  http: ^1.2.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
